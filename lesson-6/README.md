# Lesson 6 – React Router, Git, Start the Ecommerce Project

## Lesson Summary

1. **Started the Ecommerce project**
   - Using React and Vite

2. **Git**
   - Track changes in our code

3. **Routing**
   - Create multiple pages in React

4. **Created pages**
   - `HomePage`
   - `CheckoutPage`
   - `OrdersPage`

5. **Updated links**
   - Integrated with routing

6. **Separated the Header**
   - Converted into a reusable component

7. **Exercise Changes**
   - 6a In checkout page split the header into a separate component. Rename css file.
   - 6b Create checkout folder and move related files. Update import paths.
   - 6c Use `<Link>` instead of `<a>` in Checkout Header
   - 6d Save changes to Git
   - 6e Rename header.css to Header.css and save in git
   - 6f Use `<NavLink>` in Header.jsx, Adds class 'active' to current link, Style orders link to be underlined on active
   - 6g Update favicon for HomePage, OrdersPage & CheckoutPage

   **Challenge Exercises**
   - 6h Move Tracking Page to React
   - 6i Update `<a>` with `<Link>`
   - 6j Move logos and Icons folder from public/images to src/assets/images, Import the images and Use src={} wherever necessary
   - 6k Make a 404 (Not Found) Page
